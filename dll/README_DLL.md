# Patched DLL

This repo ships one patched DXVK D3D9 DLL:

- `d3d9.dll`

The setup script can apply it interactively:

```bash
./scripts/setup-waw-crossover.sh
```

Or patch manually:

```bash
./scripts/install-patched-dll.sh
```
